# This tool is used to ask the user a question
name: ask
description: Ask the user a question to clarify their intent
parameters:
  question:
    type: string
    description: The question to ask the user
    required: true
callable: 
  file: actions/ask.py
  function: ask_user
  description: Ask the user a question
  parameters:
    question: