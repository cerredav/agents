# Resolve a named location to coordinates and airport metadata when available
name: named_location_to_lat_lon_pair
description: Resolve a location name to latitude, longitude, and an IATA airport code when available
parameters:
  location:
    type: str
    description: Location or airport name to resolve
    required: true
callable: 
  file: actions/geocode.py
  function: get_geocode
  description: Resolve a location name to latitude, longitude, and an IATA airport code when available
  parameters:
    location:
      type: str
      description: Location or airport name to resolve
      required: true
